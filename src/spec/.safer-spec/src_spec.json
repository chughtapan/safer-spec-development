{
  "formatVersion": "0.1.0",
  "folder": "src/spec",
  "generatedAtSha": "16cc46e7ee40c94f6db070cdd56ce038bc78114f",
  "exports": [
    {
      "name": "ItSpec",
      "shape": "type",
      "requiredPropertyTypes": [],
      "observedPropertyTypes": [],
      "skipped": [],
      "residualContract": {
        "_tag": "some",
        "body": "fast-check seed and numRuns come from fast-check's own defaults (numRuns=100, seed via FC env or random); Vitest config does NOT propagate to fast-check, and this wrapper passes no override",
        "reason": "behavioral residue beyond the call signature; downstream authors need to know the property runner is not configured through Vitest."
      },
      "residualAssumes": [
        {
          "claim": "first positional `id` arg matches the `@spec.property` JSDoc directive value above the call site",
          "reason": "cross-check enforced by `validate --implemented`; mismatch is exit code 11 (MISSING_SPEC_PROPERTY)."
        },
        {
          "claim": "JSDoc directives above this call match `id`, `meta.type`, and `meta.exports` member names",
          "reason": "cross-check enforced by `validate --implemented`."
        }
      ],
      "residualGuarantees": [
        {
          "claim": "registers the property as a Vitest todo placeholder under `id`",
          "reason": "side-effect contract; the call mutates Vitest's collector, observable only at runtime."
        },
        {
          "claim": "registers a fast-check property under `id` that runs `body` against samples drawn from `arb`; on completion attaches `{numRuns, numSkips}` to the Vitest task's `meta.fastCheck` slot",
          "reason": "side-effect contract; reporter reads `meta.fastCheck` to build per-folder execution sidecars."
        }
      ],
      "sourceRef": {
        "path": "src/spec/grammar/it-spec.ts",
        "line": 53,
        "sha": "16cc46e7ee40c94f6db070cdd56ce038bc78114f"
      }
    },
    {
      "name": "itSpec",
      "shape": "unknown",
      "requiredPropertyTypes": [
        "Typechecking"
      ],
      "observedPropertyTypes": [
        "Inclusion",
        "Typechecking",
        "Constant Equality",
        "Constant Non-Equality",
        "Constant Bounds Checking",
        "Roundtrip"
      ],
      "skipped": [
        {
          "propertyType": "Partial Roundtrip",
          "reason": "registration sink only; there is no companion that decodes a registered test back to its metadata."
        },
        {
          "propertyType": "Commutative Paths",
          "reason": "two methods (`todo` and `prop`) cover orthogonal lifecycle states (stub vs implemented), not commuting paths."
        },
        {
          "propertyType": "Constant Equality",
          "reason": "the object is a constant export; `itSpec === itSpec` is trivially true and not a property worth gating."
        },
        {
          "propertyType": "Constant Non-Equality",
          "reason": "no anti-collision invariant between todo and prop methods."
        },
        {
          "propertyType": "Constant Bounds Checking",
          "reason": "not a numeric/length output."
        },
        {
          "propertyType": "Inclusion",
          "reason": "a method record, not a collection."
        },
        {
          "propertyType": "Roundtrip",
          "reason": "registration sink only; no inverse from a registered test back to its metadata."
        },
        {
          "propertyType": "Exception Raising",
          "reason": "registration is synchronous and total; property failures live INSIDE the test body the runner executes, not in `itSpec` itself."
        }
      ],
      "residualContract": null,
      "residualAssumes": [],
      "residualGuarantees": [
        {
          "claim": "`itSpec.todo(id, meta)` registers a Vitest todo carrying property metadata; `itSpec.prop(id, meta, arb, body)` registers a Vitest property with stats sink",
          "reason": "the runtime encoding of `@spec.*` directive metadata; the JSDoc shape and the runtime arg shape must agree, enforced by extractProperties."
        }
      ],
      "sourceRef": {
        "path": "src/spec/grammar/it-spec.ts",
        "line": 142,
        "sha": "16cc46e7ee40c94f6db070cdd56ce038bc78114f"
      }
    }
  ],
  "coverage": {
    "typeCoverage": 1
  },
  "thresholds": {
    "typeCoverage": 0.9,
    "preconditionPassRate": 0.95,
    "branchCoverageFromSpecTests": 0.85
  }
}
