{
  "name": "@chughtapan/safer-spec-core",
  "version": "0.0.0",
  "description": "Shared kinds enum + directive grammar + JSON sidecar schema + format-version for safer-spec-development",
  "type": "module",
  "license": "MIT",
  "author": "Tapan Chugh",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist/**", "README.md"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint src"
  },
  "dependencies": {
    "effect": "^3.21.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "vitest": "^3.0.0"
  }
}
